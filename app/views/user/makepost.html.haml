%h1 Create a post
%p You are allowed to use basic HTML elements.

=form_with model: @post, url: blog_createpost_path, data: {turbo:false} do |f|
  - if @post.errors.any?
    %h2 There were some errors!
    %ul
    - @post.errors.full_messages.each do |error|
      %li= error

  .field
    = f.label :title
    %br/
    = f.text_field :title

  .field
    = f.label :content
    %br/
    = f.text_area :content

  = f.submit